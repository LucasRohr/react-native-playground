import { COLORS } from '../constants';

export const screenOptions = Object.freeze({
  drawerStyle: {
    backgroundColor: COLORS.backgroundSecondary,
    paddingTop: 20,
  },
  drawerItemStyle: {
    backgroundColor: COLORS.background,
    marginBottom: 20,
  },
  drawerLabelStyle: {
    color: COLORS.primary,
  },
  headerTintColor: COLORS.primary,
});
